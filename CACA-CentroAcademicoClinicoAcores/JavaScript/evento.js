export class Evento {
  constructor(titulo, descricao, data, hora, local, imagem) {
    this.titulo = titulo
    this.descricao = descricao
    this.data = data
    this.hora = hora
    this.local = local
    this.imagem = imagem
  }
}